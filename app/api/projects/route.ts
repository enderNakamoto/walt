import { NextResponse } from "next/server";

// CRUD for projects — implemented in Phase 10
export async function GET() {
  return NextResponse.json({ message: "Not implemented — Phase 10" });
}

export async function POST() {
  return NextResponse.json({ message: "Not implemented — Phase 10" });
}
