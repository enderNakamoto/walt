import { NextResponse } from "next/server";

// POST → SSE stream of agent conversation — implemented in Phase 6
export async function POST() {
  return NextResponse.json({ message: "Not implemented — Phase 6" });
}
