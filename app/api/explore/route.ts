import { NextResponse } from "next/server";

// POST → SSE stream of exploration — implemented in Phase 4
export async function POST() {
  return NextResponse.json({ message: "Not implemented — Phase 4" });
}
